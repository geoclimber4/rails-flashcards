<h1>This was round <%= @round.id %></h1>
<h2><%= @round.deck.name %></h2>

<p>You've completed a round of <%= @round.deck.name %>. You answered <%= @round.cards.count %> on the first try. You submitted <%= @round.guesses.count %> total guesses before each card was answered correctly.</p>
